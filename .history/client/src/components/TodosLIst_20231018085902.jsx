import React from "react";

const TodosLIst = () => {
  return (
    <Paper elevation={3}>
      <List>
          <ListItem key={index}>
            <ListItemText primary={item} />
          </ListItem>
        ))}
      </List>
    </Paper>
  );
};

export default TodosLIst;
